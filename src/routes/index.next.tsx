// @ts-nocheck

import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/index.next")({
  component: IndexNextDraftRoute,
});

function IndexNextDraftRoute() {
  return null;
}
